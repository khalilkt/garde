version: "3"

services:
  backend:
    build:
      context: ./garde_backend
    command: gunicorn garde_backend.wsgi:application --bind 0.0.0.0:8000
    # command: python manage.py runserver 0.0.0.0:8001
    ports:
      - "8001:8001"
    volumes:
      - django_app:/app

  frontend:
    build:
      context: ./garde_frontend
    volumes:
      - react_build:/react/build
  nginx:
    image: nginx:latest
    ports:
      - "80:8080"
    volumes:
      - ./nginx-setup.conf:/etc/nginx/conf.d/default.conf:ro
      - react_build:/var/www/react
    depends_on:
      - backend
      - frontend

volumes:
  react_build:
  django_app:
